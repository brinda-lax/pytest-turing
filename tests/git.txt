# Step 1: Initialize git if not already done
git init

# Step 2: Add all files to staging
git add .

# Step 3: Commit with a message
git commit -m "Initial commit with all tasks and tests"

# Step 4: Set your remote repository
git remote add origin https://github.com/brinda-lax/pytest-turing.git

# Step 5: Push to the `main` branch (or `master`, check GitHub)
git push -u origin main
